# 待办（按商店评论整理，2026-09-25）

来源 `docs/store-reviews-2026-09.md`。顺序按"用户损失 × 提及次数 ÷ 成本"排，每条写清怎么验证；没有验证方法的不开工。
已在 v1.2.26 做掉的不列（大文档性能、阅读模式、大纲跳转与跟随、切换崩溃、多语言）。

## 第一批：小而准，评论里点名的

| # | 事项 | 依据 | 做法 | 验证 | 估时 |
|---|---|---|---|---|---|
| 1 | 保存改为原子写入 | 1 条 "死机后文件被覆盖成 0K"，最伤用户 | 先写临时文件再 rename 替换；写入失败保留原文件 | 单元测试：写入中途抛异常，原文件内容不变 | 已做 2026-09-25：原子写入早已有；补了"编辑器未回话时不把空缓冲区写到非空文件上"和备份原子化，Tools/SafeFileCheck 证明失败不截断 |
| 2 | `==文本==` 高亮 | 3 条 + 1 条 | Muya 行内规则 + 渲染 `<mark>` + 导出/复制保留 + 光标穿越行为 | CommonMark 例子集加一组；EditorBench 加往返检查 | 已做 2026-09-25：编辑器规则、导出词法、HTML 导入、大纲标题、主题各一处；inline-mark-check.js 验证 |
| 3 | 页内锚点 / Ctrl+点击链接不跳转 | 5 条 | `#anchor` 链接按 slug 定位滚动；外链照常 | outline-jump 同类检查改成点链接 | 已做 2026-09-25：按导出同款 slug 匹配标题；在 mousedown 上跟随（click 时块已重渲染）；光标未移动不再触发跟随滚动；anchor-link-check.js |
| 4 | 深色模式分割线、表格边框；浅色行内代码对比度 | 3 条 | 主题 CSS 变量补齐 | 截图基线（两套主题各一张）比对 | 已做 2026-09-25：行内代码独立变量，三套主题的表格边框和分割线加深；theme-contrast-check.js 按对比度验证 |
| 5 | 粘贴代码缩进不是 4 空格；代码块字号不跟字号设置 | 2 条 | 粘贴时按 TabSize 展开；代码块字号继承 | 粘贴检查；布局基线 | 已做 2026-09-25：tab 宽度跟设置（Prism 主题原本写死 2），源码模式字号和 tab 跟设置，宿主改 TabSize 即时下发；code-font-check.js |

| 1a | 关窗后进程留在后台、下次点图标没有窗口 | 你的反馈；安装被占用也是它 | 已做 2026-09-25：退出与否按剩余窗口判断（原按弱引用列表，未回收即视为有窗口）；交接给运行中实例的等待有上限，超时或对方无法开窗则自行启动，对方退出 | hp 日志 |

## 第二批：打印与导出

| # | 事项 | 依据 | 做法 | 验证 | 估时 |
|---|---|---|---|---|---|
| 6 | 打印崩溃 | 5 条，跨三年，含非管理员账户临时文件权限 | 临时文件放用户目录；打印异常捕获并提示而不是崩 | hp 上普通账户跑一遍；异常注入 | 已做 2026-09-25：临时文件改到应用自己的目录；浏览器打印对话框失败退到系统对话框；WebView 进程死亡时预览关闭并报错、编辑器自动重载。崩溃本身未复现，只能防御 |
| 7 | 打印所见即所得 | 你的反馈 | 打印编辑器页面本身，加打印样式隐藏编辑辅助元素 | print-check 扩展：打印 HTML 与编辑器同一份样式 | 已做 2026-09-26：打印编辑器页面本身，打印样式去掉编辑辅助元素、关闭屏幕外不排版、白底、代码换行；并补回代码框/引用条/表格边框（编辑器样式在 @media not print 下被禁用，打印时强制显示背景）；print-check.js 断言这些块可见 |
| 8 | PDF 带目录书签；图片丢失；公式字体 | 5 条 | 导出走 WebView2 的 PrintToPdf 并带 outline；图片路径绝对化 | 导出后用 pdf 解析器检查书签数、图片数 | 已做 2026-09-26：PDF 走 CDP printToPDF 带文档大纲（书签）；导出把 KaTeX 字体和图片改成绝对地址；export-pdf-check.js 真出一份 PDF 验证大纲和图片 |

### 反馈补充（2026-09-26）

| 事项 | 现状 |
|---|---|
| 打印代码框/引用/表格样式丢失 | 已做 0bb55bb |
| 单个 ~ 被导出 PDF 当成删除线（1~6、2~5） | 已做：导出词法改为只认 ~~；export-tilde-check.js |
| 导出 PDF 默认文件名同源文件名不同后缀 | 已做：SuggestedFileName 取源文件基名 |
| 导出 PDF 书签 | 引擎与参数已验证；应用端最终字节未自证，待实机导出一次读日志确认 |
| 目录链接 [文字](#标题) 点了不跳（带点号的锚点如 #1.1-x） | 已做：锚点匹配加宽松比较（忽略点号/空格/连字符）；anchor-link-check.js 补该用例 |
| 输入 <br></br> 丢字符、切阅读模式再切回编辑器消失 | 已做：空元素（br/hr/img/…）不再和随后的闭合标签配对；void-html-check.js 覆盖 15 种写法 |
| <br> 在阅读模式显示字面量 | 已做：br 源码用和其它标记一样的隐藏规则，阅读模式只显示换行 |

## 第三批：编辑行为（都在 Muya 深处，成本高）

| # | 事项 | 依据 | 做法 | 验证 | 估时 |
|---|---|---|---|---|---|
| 9 | 写完代码块直接保存会丢失 | 1 条 | 复现：保存前 flush 是否把未提交的代码块内容带上 | 往返检查加"光标在代码块内保存" | 半天 |
| 10 | 选中段落改标题级别波及相邻段 | 1 条 | 复现后定位 updateParagraph 的选区边界 | 编辑操作检查 | 1 天 |
| 11 | 列表格式切换异常，后文变引用 | 1 条 | 同上 | 同上 | 1 天 |
| 12 | 撤销闪烁、定位错；粘贴后切模式内容消失 | 2 条 | 历史栈按 loadId 隔离；切模式前 flush | 撤销检查 | 1 天 |
| 13 | Ctrl+方向键扩选、Ctrl+F 无反应、滚轮不动 | 3 条 | 逐条复现，多半是旧版问题 | 手测 | 半天 |
| 14 | 表格：删空后无法输入；单元格内换行 | 2 条 | 空单元格保留占位；Shift+Enter 插 `<br>` | 编辑操作检查 | 1 天 |

## 第四批：启动、文件树、图片

| # | 事项 | 依据 | 做法 | 验证 | 估时 |
|---|---|---|---|---|---|
| 15 | 缺 WebView2 Runtime 的提示与引导 | 2 条 | 启动检测，缺失时给下载链接而不是空白 | 卸载 Runtime 的虚拟机上跑 | 半天 |
| 16 | 启动耗时 | 3 条 "启动慢" | 先量：从进程起到编辑器可输入的毫秒数写进日志 | 日志数字 | 半天量，改动另算 |
| 17 | 文件树：单击预览双击打开；显示 txt；外部修改提示 | 4 条 | 部分已做：显示/打开 .txt/.text/.log（文件树、搜索、拖放、命令行都放行）；单击预览、外部修改提示待做 | — / 各半天 |
| 18 | 网络图片不显示 | 2 条 | 复现：WebView2 的 file:// 页面加载 http 图片的策略 | 图片加载检查 | 半天 |

## 第五批：功能

| # | 事项 | 依据 | 估时 |
|---|---|---|---|
| 19 | 源码/预览分栏 | 3 条 | 3 天 |
| 20 | 多主题、可选字体、字体颜色 | 13 条（多为主题） | 2 天 |
| 21 | 代码块行号 | 2 条 | 1 天 |
| 22 | 导出 docx | 2 条 | 2 天 |
| 23 | 大纲与文件树同时显示 / 侧栏放右侧 | 3 条 | 1 天 |

## Uno 版

| # | 事项 | 估时 |
|---|---|---|
| U1 | 大纲当前标题高亮（StateChange cur + OutlineCurrent），高亮行滚进可视区 | 半天 |
| U2 | 阅读模式标识（标题栏、状态栏） | 小 |
| U3 | 八种语言补齐 25 条字符串 | 半天 |
| U4 | 从文件面板打开后把焦点交给编辑器 | 小 |

## 结构性

| # | 事项 | 估时 |
|---|---|---|
| S1 | 退出设置不重载页面：先给 TabSize 和六个 AutoPair/列表/表格选项补运行中处理，每项加检查，再关掉重载 | 1 天 |
| S2 | 大文档源码模式（三十万字符以上默认源码模式） | 2 天 |

| Ctrl+S 保存后变成编辑 xxx.md~RF….TMP | 已做 0424a7e：原子保存内部重命名不再被文件监视器当成用户改名 |

| 保存在同步盘旁生成 tmp/~RF 文件 | 已做：改用 File.Move 原子替换（不再有 ~RF 备份文件）；临时文件是隐藏+临时属性的点文件，同步盘会跳过；仅在替换的瞬间存在 |

## 小优化（2026-09-26，均已核对当前状态）

| # | 建议（评论） | 核对结果 | 做法 | 估时 |
|---|---|---|---|---|
| S1 | 复制代码块后给反馈（2 条） | 已做：复制后按钮短暂显示对号（1.5 秒）；dbg 验证点击后加 ag-code-copied 类 | — |
| S2 | 应用内反馈入口失效（1 条） | 反馈按钮在 Visibility=Collapsed 面板里、且 POST 到原作者服务器（AllowOutboundNetwork=false 已关） | 已改：关于页去掉原应用商店/致谢链接，换成 GitHub（贡献/提 issue 的去处） | 已做 |
| S3 | 开始菜单搜 "markdown" 搜不到（1 条） | 已是：exe FileDescription/AssemblyTitle="Typedown - Lightweight Markdown Editor"，快捷方式 Comment="Lightweight Markdown Editor"，Windows 搜索会索引这些 | 无需改 |
| S4 | 锚点 HTML `<a id="x"></a>` 原样显示（1 条） | 已做：空的行内元素源码在阅读模式隐藏（原来无子节点时被强制常显）；void-html-check.js 加锚点用例 | — |
| S5 | 默认字体颜色偏深刺眼（1 条） | 已做（调柔）：浅色正文由纯黑改为 #24292e；可调颜色的完整设置归入主清单 20 | 半天 |
| S6 | 粘贴纯文本被识别成代码块（2 条） | 待复现粘贴判定 | 粘贴逻辑收紧对"看起来像代码"的判定 | 半天，待复现 |
| S7 | 图片缩放不自由 / img width 无效（2 条） | 待核对 zoom/width 处理 | 支持自由缩放或让 width 生效 | 1 天，待复现 |
| S8 | 多个空行被合并（1 条） | Markdown 规范行为 | 作为选项：保留多空行 | 半天 |
| S9 | 代码高亮补 vue/jsx（2 条） | 已核对：jsx/tsx（react）本就按需加载、已高亮；vue 不在 Prism。已把 vue 别名到 markup，至少高亮模板；dbg 验证 vue 0→13 tokens | — |
| S10 | YAML front matter 渲染异常（1 条） | 已复现：当前渲染为带 yaml 高亮的 front-matter 框、往返不变，未见异常；缺原评论的具体文档，暂判定不可复现 | 不改 |
| S11 | LaTeX `\color{rgb()}` 不支持（1 条） | 确认：KaTeX 只认命名色和 #hex，rgb() 非其语法；需预处理 rgb()→#hex 或加宏 | 低优先，1 条 2022 年，暂缓 |


## 交互反馈（2026-09-26，第二批）

| 事项 | 做法 | 状态 |
|---|---|---|
| 复制打钩短暂替换按钮（不再半透明/移开就消失） | 复制期间按钮 opacity 强制为 1，只显示对号 | 已做 |
| 搜索失焦后再按快捷键无法重新聚焦 | Search 每次都发聚焦请求，查找栏收到就聚焦并全选搜索框 | 已做 |
| 菜单项快捷键未显示（切换最近使用的 tab） | LastUsedTab 改用菜单项重载注册，显示快捷键 | 已做 |
| 反馈按键引导到 GitHub issues | 关于页反馈按钮显示出来，点击打开 flintt/Typedown/issues | 已做 |
